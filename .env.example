# Copy to .env and fill in your SMTP and Stripe details.

PORT=3000
SMTP_HOST=smtp.ipa.net
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password
FROM_EMAIL=OSWater@ipa.net

# Stripe (required for bill pay). Get keys from https://dashboard.stripe.com/apikeys
STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_SECRET_KEY=sk_test_...
